[user]
	name = Eric Pruitt
	email = eric.pruitt@gmail.com
	signingkey = F8601B5D2511B4C3535232488DDDE2E6053692AB

[core]
	excludesfile = ~/.gitignore

[alias]
	wdiff = diff --color-words
	wlog = log --word-diff=color -p --diff-filter=M
	cdiff = diff --color-words --word-diff-regex=.
	cshow = show --color-words --word-diff-regex=.
	clog = log --word-diff-regex=. -p --diff-filter=M
	what = colorized-blame what -dc
	when = colorized-blame when -dc
	who = colorized-blame who -dc
	heat = colorized-blame what -dc --heat-map
	mine = log --author=^Eric\\ Pruitt\\ < -p
	others = log --author=^Eric\\ Pruitt\\ < -p --invert-grep

[pager]
	colorized-blame = true

[color]
	diff = true

[push]
	default = upstream

[grep]
	patternType = perl

[diff]
	algorithm = patience

[include]
	path = ~/.local.gitconfig

[commit]
	verbose = true

[advice]
	statusHints = false

[rerere]
	enabled = true
