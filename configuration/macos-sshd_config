# Only allow key-based SSH authentication.
ChallengeResponseAuthentication no
PasswordAuthentication no

# Accept locale environment variables and common settings from clients.
AcceptEnv EDITOR LANG LC_* LESS LS_COLORS PAGER TZ VISUAL

# The values below this line are defined in the sshd_config that ships with
# macOS 10.13 which are also the defaults in the upstream OpenSSH release:

# - The default is to check both .ssh/authorized_keys and .ssh/authorized_keys2
#   but this is overridden so only check .ssh/authorized_keys is checked.
AuthorizedKeysFile .ssh/authorized_keys

# - Override default of no subsystems.
Subsystem sftp /usr/libexec/sftp-server
